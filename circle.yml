dependencies:
  pre: 
   - sudo apt-get update -y && sudo apt-get install -y maven && sudo apt-get install -y openjdk-7-jdk; 
   
  post:
   - mvn clean package -DskipTests

test:
  override:
    - /bin/true
